\documentclass[border=1pt]{standalone}
\usepackage{fontspec}
\usepackage[dvipsnames,svgnames]{xcolor} % Extended color support
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{unicode-math}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{enumitem}

\pgfplotsset{
  compat=1.18
}

\setmainfont{Libertinus Sans}
\setsansfont{Libertinus Sans}
\setmonofont{Libertinus Mono}
\setmathfont{Libertinus Math}

\usetikzlibrary{
  calc,
  positioning,
  intersections,
  through,
  angles,
  quotes,
  arrows.meta,
  patterns,
  decorations.pathreplacing,
  decorations.markings,
  decorations.text,
  shapes,
  shapes.geometric,
  matrix,
  fit,
  backgrounds,
  fadings
}

\begin{document}
\begin{tikzpicture}[>=Latex]
  \tikzset{
    % every path/.style={thick},
    % every node/.style={font=\large}
  }
  \coordinate (x1) at (0,0);
  \coordinate (x2) at (2.5,0);
  \coordinate (x3) at ($(x1)!-0.3!(x2)$);
  \coordinate (x4) at ($(x1)!1.3!(x2)$);
  \draw[<->,thick] (x3)--(x4) node[midway, above=25pt] (label1)
  {names of points};
    \draw[<->,thick] (x3)--(x4) node[midway, below=25pt] (label2)
  {coordinates of points};
  \fill[color=black] (x1) circle (2pt);
  \fill[color=black] (x2) circle (2pt);
  \node (labelX1) at (x1) [below=2pt] {$x_1$};
  \node (labelX2) at (x2) [below=2pt] {$x_2$};
  \node (labelA) at (x1) [above=2pt] {$A$};
  \node (labelB) at (x2) [above=2pt] {$B$};
  \draw[->] (label1.-165)--(labelA.north);
  \draw[->] (label1.-15)--(labelB.north);
  \draw[->] (label2.165)--(labelX1.south);
  \draw[->] (label2.15)--(labelX2.south);
  \end{tikzpicture}

\end{document}
